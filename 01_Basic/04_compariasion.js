console.log(10 > 1); // it will return true because 10 is greater than 1
console.log(10 < 1); // it will return false because 10 is not less than 1
console.log(10 >= 1); // it will return true because 10 is greater than 1
console.log(10 <= 1); // it will return false because 10 is not less than 1
console.log(10 == 10); // it will return true because both are equal
console.log(10 != 10); // it will return false because both are equal
console.log(10 === 10); // it will return true because both are equal and same datatype
console.log(10 !== 10); // it will return false because both are equal and same datatype
console.log(10 == "10"); // it will return true because both are equal but different datatype
console.log(10 === "10"); // it will return false because both are equal but different datatype
console.log(10 != "10"); // it will return false because both are equal but different datatype
console.log(10 !== "10"); // it will return true because both are equal but different datatype

console.log(true == 1); // it will return true because true is equal to 1
console.log(false == 0);
console.log(true === 1); // it will return false because true is not equal to 1
console.log(false === 0);
console.log(true != 1); // it will return false because true is equal to 1
console.log(false != 0);
console.log(true !== 1); // it will return true because true is not equal to 1
console.log(false !== 0); // it will return true because false is not equal to 0